# Cryptography toolkit

Just learning about cryptography, techniques like hashing (SHA-256) for verifying file integrity,
symmetric encryption (AES), and asymmetric encryption (RSA) using public and private keys.

The main idea is to build a fully functional command-line cryptography tool in Python.